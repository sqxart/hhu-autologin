# 更新日志

## v1.4.2 — 2026-09-16

- **高 DPI 适配**：GUI 启动即向 Windows 声明 DPI 感知（Per-Monitor v2，逐级降级兼容老系统）——修复系统缩放 125%/150% 下整个界面被位图拉伸发虚的问题；窗口尺寸、日志字体、长文案折行宽度按真实缩放比放大；小屏笔记本 / 超高缩放（窗口放不下屏幕）自动切换紧凑布局（压缩日志区与区块间距），保存按钮不再被屏幕底边吃掉
- **窗口图标多分辨率**：任务栏/标题栏图标提供 32/48 两档，高分屏更锐利
- **CI 版本矩阵**：单测在 Python 3.8 / 3.10 / 3.12 × Windows / Ubuntu 上每次推送自动跑——README 承诺的"3.8+"从口号变成持续验证
- README 明确系统要求：打包版 exe 需 Windows 10（1607+）64 位；源码方式 Windows/macOS/Linux + Python 3.8+

## v1.4.1 — 2026-09-16

- **修复：代理软件导致"不在校园网"误报 / 守护瘫痪**（新手实测发现的阻断级 bug）：Clash 等代理开启系统代理时，HTTP 客户端默认跟随系统代理，发往校园网认证门户的内网请求被送进代理后全部被拒（ConnectionRefused 10061）——GUI「一键抓取」误报"不在校园网"，守护整条登录链路不可用。现在 `hhu_login` 与 `hhu_campus_report` 的**所有请求强制直连**（空 `ProxyHandler` 顶掉系统代理）：Clash 开不开都不影响，"先登录再开 Clash"的老规矩不再需要；掉线探测也不再会被代理"代答"造成假在线。回归测试锁定该行为
- 便携包附件名改 ASCII（`hhu-autologin_v1.4.1_portable.zip`）——GitHub 会删除附件名里的非 ASCII 字符；zip 内部文件夹与说明书保持中文

## v1.4.0 — 2026-09-16

- **可视化控制台 `hhu_gui`**（tkinter，零第三方依赖）：状态监视、一键抓取账号与服务、账密表单、开机自启开关（计划任务注册/卸载）、检测间隔调节、立即检测登录状态、退出当前校园网账号、断路保护提示与重置
- **实时日志窗口**：内嵌显示守护日志尾部，每 2 秒自动滚动更新（心跳 / 掉线 / 登录记录一目了然）
- **双主题**：默认浅色 / 暗黑（深色工业风，遵循 ak-ui 设计契约：中性面层 + 黄色行动信号 + 终端感遥测配色）
- **系统托盘常驻**：关闭窗口缩到托盘（气泡提示），托盘菜单打开窗口 / 立即检测 / 退出；常驻期间内嵌守护循环（与计划任务共存幂等）
- **自动退出选项**：`auto_exit_after_login`（上线即退，满足"只想开机登录一次"）与 `auto_exit_minutes`（定时退出），均已实测
- **方舟风图标**：海蓝斜切底板（#1664A4 / #0D4D82，河海校色）+ 黄色 H + 绿色在线点（像素绘制超采样抗锯齿，内存 PNG，零资源文件），窗口、任务栏、托盘三处统一
- **校区选择**：账号配置区可选金坛/江宁/西康路——金坛用实测静态配置，其他校区自动抓取当前服务列表填充下拉（服务列表跟随所在校区网络下发）；保存时自动转为跨校区通用的运营商关键词
- **守护停止语义**：托盘菜单动态显示「停止/恢复后台守护」（停止=卸载计划任务+暂停内嵌循环，控制台保留）；「上线后自动停止守护」与「守护运行 N 分钟后自动停止」均停止守护而非退出控制台
- **通知开关**：「设置」区新增两个开关——掉线自动恢复时通知 / 自动登录失败时通知，写入 `config.ini` 的 `[notify]` 节。此前通知虽默认全开，但界面上无法调整、保存配置时还会被强制打开
- **托盘图标修复**：旧实现用 `CreateIcon` + 24 位原始位图，实测托盘图标**全黑**（颜色数据在 24→32 位转换中丢失，且 24 位无 alpha 无法表达抗锯齿）；改为多尺寸 PNG 打包成 `.ico` 后用 `LoadImage` 加载，带真实 alpha，并按屏幕 DPI 自动选用 16/20/24/32px
- **断路保护文案重写**：按钮「清除断路器」→「重置断路保护」，下方补充「登录时密码错误触发断路保护，不再尝试登录」；触发提示与弹窗同步改写，并明确「保存配置会自动重置」
- **便携版发布包**：`使用前必读！！！.txt` + exe 一起打包为 `hhu-autologin_v1.4.0_portable.zip`（附件名 ASCII——GitHub 会删除附件名里的非 ASCII 字符；zip 内部文件夹与说明书保持中文），新手解压即用（不需要装 Python）；「使用前必读」按小白视角写清三步上手、蓝色警告窗口、Clash 代理坑、断路保护含义与彻底卸载方法
- **源码运行无黑窗**：新增 `hhu_gui.pyw` 入口——双击 `.py` 会先弹一个黑色控制台窗口（Windows 把 .py 关联到带控制台的 python.exe），双击 `.pyw` 走 pythonw 无控制台，直接出界面；启动失败会弹消息框提示（pythonw 下没有控制台可看报错）
- **采集工具免装 Python**：CI 一并构建 `hhu_campus_report.exe`，可直接发给其他校区同学运行
- **采集脚本加固**：打包成 exe 后输出路径改用 exe 所在目录（原用 `__file__`，打包后会写进临时解包目录并随进程消失）；报告新增「运行环境」行；控制台不再强行改 UTF-8 编码（避免中文在旧控制台花屏）；Python 版本低于 3.8 时给出中文升级引导
- 界面底部挂 GitHub 项目地址，点击直达仓库
- GitHub Actions 打包新增 `hhu_gui.exe`（--windowed）、`hhu_campus_report.exe` 与便携版 zip
- 修复 GUI 中 run_once 因运行时 CONFIG 未填充而崩溃的问题（交互实测发现）；「打开日志文件夹」改用 explorer 子进程，失败时给出路径提示

## v1.3.0 — 2026-09-15

- **配置向导 `--setup`**：在线时自动抓取账号与当前服务（getOnlineUserInfo），只需输入密码（回显保护 + 两遍确认）
- 换服务商免重输密码：账号未变时自动沿用已保存密码；向导写完配置自动清除断路器
- 非校园网环境（手机热点等）运行有明确提示，转为手动填写；服务列表拉取失败同样降级
- 修复向导密码输入在 Windows 管道/重定向下挂起的问题（非终端时改用可见输入）

## v1.2.0 — 2026-09-15

- **运营商服务支持**：服务列表改由 `getServices` 接口拉取（登录页原始 HTML 无服务列表，此前仅靠默认值兜底），移动 / 联通已实测登录成功（getOnlineUserInfo 的 realServiceName 确认），电信同流程
- **跨校区自动适配**：运行时拉取当前校区服务列表后按关键词匹配，`service = 联通` 这类配置换校区无需修改

## v1.1.0 — 功能随 v1.4.0 一并发布

- 系统通知（可配置）：登录成功/失败弹 Windows Toast，带失败冷却防刷屏
- 心跳日志：在线时每分钟一行，日志最后一行时间即守护存活时间
- SSID 跳过、服务未匹配等情况写日志
- logout 改用 userIndex 方式（logoutByUserIdAndPass 实测被拒）

## v1.0 — 2026-09-13

首发版本。

- 掉线自动重登：每分钟检测，在线秒退，掉线直连 ePortal 接口登录（无浏览器）
- 登录页服务动态解析（支持校园网/移动/电信/联通，显示名与提交值不一致自动纠正）
- 参数双重 URL 编码，与页面 `doauthen()` 逐字节对齐
- 断路器：认证被拒即停止重试，防止触发验证码锁定
- SSID 门卫：仅在校内 WiFi 工作（可选）
- `--check` 自检 / `--logout` 注销 / `--loop` 循环 / `--clear-flag`
- GitHub Actions 自动打包 Windows exe
